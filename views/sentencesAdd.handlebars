<h2>Your word is: <b>{{word}}</b>, now make some magic!</h2>
<p>Definition: {{definition}}</p>
<form class="new_sentence" id="new_sentence" action="/sentences" accept-charset="UTF-8" method="post">
  <h2>Countdown Clock</h2>
    <div class="timer">
      <h3>Timer</h3>
      <progress value="0" max="20" id="progressBar"></progress>
    </div>
  </div>
<p>
  <textarea placeholder="Your sentence" name="life_story" id="cat_life_story">
  </textarea><br><br>
  <input type="submit" class="button" name="commit" value="Create Sentence" />
</p>

</form>

<script>
  var timeleft = 20;
  var downloadTimer = setInterval(function(){
   document.getElementById("progressBar").value = 20 - --timeleft;
   if(timeleft <= 0)
     clearInterval(downloadTimer);
  },1000);
  if(value === 0){
  }
</script>
<a class="link" href="/">Cancel</a>
